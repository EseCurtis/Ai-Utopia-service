import { TScale } from "@/dtos/Scales";

export type TCharacter = {
    joy: TScale;
    anger: TScale;
    sadness: TScale;
    excitement: TScale;
    calmness: TScale;
    confidence: TScale;
    fear: TScale;
    kindness: TScale;
    honesty: TScale;
}
